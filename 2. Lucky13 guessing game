# lets import a random module that comes with python
# understand how range(start, stop)

import random 
r_number = random.randrange(0, 13)

# print(r_number) simply tests that a new random number being loaded 
# make the user guess the number, to match the random number generated 
# add a counter
# also give the user some direction how to guess next time 

attempt = 0
# note this will be a loop
while True:
    attempt += 1
    user_guess = input("Welcome to Lucky13 , make a guess from 1-13: ")
# make sure the user_guess is not under 0 or a string 
    if user_guess.isdigit():
        user_guess = int(user_guess)
    else:
        print("Please try again")
        continue
    if user_guess == r_number:
        print(" Congradulations ! Lucky13 you got it ")
        break # this will break out of the while loop at the top to prevent an infinate loop
    else:
        if user_guess > r_number:
            print("high Guess")
        else:
            print("Low Guess")

print("Got it, in", attempt, "attempts:)")
